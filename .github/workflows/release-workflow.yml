name: Website Release Pipeline

on: 
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:
    inputs:
      refBuildOverride:
        type: string
        description: 'Branch, tag or commit SHA1 to build'
        default: ''
      forceDeploy:
        type: boolean
        description: 'Force Deploy'
        default: true
      logLevel:
        type: choice
        description: 'Log Level'
        default: 'error'
        options:
        - info
        - warning
        - error

env:
  REF_TO_BUILD: ${{ input.refBuildOverride != '' && inputs.refBuildOverride || github.ref_name }}
  IS_PR_GITHUB_EVENT: ${{ github.event_name == 'pull_request' && 'true' || 'false' }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository and submodules
        uses: actions/checkout@v3
        with:
          submodules: recursive
          # ref: ${{ inputs.refToBuild }}
          ref: 'master'

      - name: DEBUG echo github
        run: echo ${{ github.ref_name }}

      - name: DEBUG echo refToBuild
        run: echo ${{ inputs.refToBuild }}

      - name: DEBUG echo REF_TO_BUILD
        run: echo ${{ env.REF_TO_BUILD }}

      - name: Setup node 14
        uses: actions/setup-node@v2
        with:
          node-version: 14.x

      - run: npm install

      - run: npm run build

      - name: Copy Build to Prod
        if: ${{ env.IS_PR_GITHUB_EVENT == 'false' }}
        uses: garygrossgarten/github-action-scp@release
        with:
          local: dist/edison-earnest-site
          remote: /var/www/html
          host: ${{ secrets.HOST }}
          username: ${{ secrets.SSH_USER }}
          password: ${{ secrets.PASSWORD }}